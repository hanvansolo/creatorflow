// TODO: Rewrite for Footy Feed (stubbed during F1→Soccer migration)

export const TRACK_SVGS: Record<string, string> = {};
export const DEFAULT_TRACK_SVG = '';

export function resolveCircuitSlug(_name: string): string {
  return '';
}
